## running workflow using container
name: CI
on:
  push:
    branches: [ main ]
jobs:
  container-test-job:
    runs-on: self-hosted
    container:
      image: docker.io/rkalluru/learnk8s:rhel9-bare
    steps:
      - name: messsage hello world
        run: echo hello